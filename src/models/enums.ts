/* eslint-disable no-unused-vars */
export enum SortOrder {
  DSC = 'DSC',
  ASC = 'ASC'
}

export enum SortKey {
  CREATED = 'created',
  AUTHOR = 'by'
}

export enum UpdateType {
  UPDATE = 'UPDATE',
  COMMENT = 'COMMENT',
  RATING = 'RATING'
}
