const update = (by, text) => {
  return ({
    by,
    text,
    created: 'that time'
  })
}

export default {
  updates: [
    update('Jar Jar', 'Misa like'),
    update('Jar Jar', 'Misa like'),
    update('Jar Jar', 'Misa like'),
    update('Jar Jar', 'Misa like')
  ]
}
